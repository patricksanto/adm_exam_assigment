**Error**: DBT run error lock on duckdb file

Solution: disconnect dbcode from database + reload window (ctrl+shift+p - reload window)
If cannot find anything but lock still exists: re-open locally, then re-open container.
